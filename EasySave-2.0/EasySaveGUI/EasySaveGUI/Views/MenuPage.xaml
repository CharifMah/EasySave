<Page x:Class="EasySaveGUI.Views.MenuPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EasySaveGUI.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MenuPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>

        <TabControl x:Name="tabControl" Grid.Column="0" Grid.ColumnSpan="3" Grid.RowSpan="2" Grid.Row="2">
            <TabItem Header="Web Element" x:Name="WebElementsTab">
                <DataGrid x:Name="WatchDataGrid" ItemsSource="{Binding WebElements}">
                </DataGrid>

            </TabItem>
            <TabItem Header="History" x:Name="HistoryTab">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="10*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1*"/>
                        <RowDefinition Height="1*"/>
                        <RowDefinition Height="10*"/>
                    </Grid.RowDefinitions>

                    <Button Content="Start" Grid.Column="1" Grid.Row="1"/>
                    <Button Content="Stop" Grid.Column="2" Grid.Row="1"/>
                    <Button Content="Clear" Grid.Column="3" Grid.Row="1"/>

                </Grid>
            </TabItem>

            <DataGrid x:Name="JobDataGrid" ItemsSource="{Binding JobManager}" Grid.Row="1"/>
    </Grid>
</Page>
